<% if user.profile_image.attached? %>
  <!-- Image configured by user -->
  <%= image_tag user.profile_image, class: "inline-block h-8 w-8 rounded-full", alt: user.name %>
<% else %>
  <!-- "Person" icon" -->
  <%= render_icon("person", classes: "mr-2 size-6") %>
<% end %>