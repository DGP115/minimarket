<%# dom_id() is a rails helper %>
<%# dom_id(rails model object), as in dom_id(review), evaluates to id="review_'the object's id'" %>
<%#   e.g. id=review_5 %>
<%#   Thus a unique turboframe id is created per review object, creating a specific target for %>
<%#   the review model's broadcast (of a change) %>
<%= turbo_frame_tag dom_id(review) do %>
  <blockquote class="pl-4 mt-4 mb-1 italic font-normal text-gray-700">
    <%# simple_format is a Rails helper that displays textarea content while maintaining line breaks etc. %>
    <%= simple_format(review.body) %>
  </blockquote>
  <blockquote class="block pl-4 mb-1 ml-8 text-sm font-normal">
    <% if review.user.name != nil %>
      <%= review.user.name %>
    <% else %>
      <%= review.user.email %>
    <% end %>
  </blockquote>
<% end %>
